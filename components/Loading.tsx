import React from "react";

const Loading = () => {
  return (
    <div className="h-screen flex items-center bg-primary justify-center">
      <span className="text-foreground">Loading...</span>
    </div>
  );
};

export default Loading;
